<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.finalyproject">

    <application

        android:allowBackup="true"
        android:icon="@drawable/warehouse"
        android:label="@string/app_name"
        android:roundIcon="@drawable/warehouse"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".UpdataActivity"
            android:label="Ürün Güncelle"
            android:theme="@style/Guncelle" />
        <activity
            android:name=".AllProductActivity"
            android:label="Stok Bilgisi"
            android:theme="@style/AllProduct" />
        <activity
            android:name=".AddProductActivity"
            android:label="Ürün Ekle"
            android:theme="@style/AddProduct" />

        <provider
            android:name=".DataBase.Provider"
            android:authorities="com.example.user.finalyproject"
            android:exported="false" />

        <activity android:name=".DetailActivity" />
        <activity android:name=".SpleshActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainPageActivity" />
        <activity
            android:name=".DeleteActivity"
            android:label="Ürün Sil"
            android:theme="@style/DeletePage" />
        <activity
            android:name=".UrunGuncelleActivity"
            android:label="Ürün Güncelle"
            android:theme="@style/Guncelle" />
        <activity android:name=".SingUpActivity"></activity>
    </application>

</manifest>